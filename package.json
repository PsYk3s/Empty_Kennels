{
  "name": "spca-adoptions",
  "private": true,
  "workspaces": ["api", "web", "shared"],
  "scripts": {
    "dev": "concurrently -n api,web \"npm:dev -w api\" \"npm:dev -w web\"",
    "build": "npm run build -w api && npm run build -w web"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}